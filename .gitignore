.DS_Store
node_modules/

# Full-size photo master. assets/darien.jpg is the web derivative that ships;
# keeping the original out of the repo so it doesn't carry an unused 400KB.
assets/Headshot.jpg

# Logo master + a saved BBB profile page — neither is a site asset.
# assets/logos/controlled-comfort.png is the derivative that ships.
assets/controlled_comfort.avif
assets/*.htm
